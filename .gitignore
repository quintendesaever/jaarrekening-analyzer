# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.venv/
venv/
env/
*.egg-info/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
dist-ssr/
*.tsbuildinfo

# Local persisted ratio overrides (admin save)
backend/data/

# OS & editors
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.suo
*.sw?

# Cursor (local IDE state)
.cursor/

# Local git hooks (niet op GitHub)
.githooks/

# Tests: keep local extras (PDFs, scratch) out of git; track pytest modules
backend/tests/*
!backend/tests/test_*.py

# Secrets
.env
.env.*
!.env.example

# Docker / local server deploy (niet op GitHub)
Dockerfile
docker-compose.yml
docker-compose.*.yml
Caddyfile
.dockerignore
DEPLOY.md
